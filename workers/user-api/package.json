{
  "name": "datagraphe-user-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "test": "node --test test/*.test.mjs",
    "check": "wrangler deploy --dry-run --outdir dist-worker",
    "d1:local": "wrangler d1 migrations apply datagraphe-users --local"
  },
  "dependencies": {
    "@clerk/backend": "3.17.0"
  },
  "devDependencies": {
    "wrangler": "4.128.0"
  }
}
